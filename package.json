{
  "name": "api",
  "version": "1.0.0",
  "description": "* npm run start to run pro environment  * npm run docker-build to build image on docker * npm run docker-compose to build-compose on docker * npm run dev to run dev environment  * npm run test to run tests",
  "main": "index.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon -L src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@graphql-tools/schema": "^8.3.13",
    "@prisma/client": "^3.14.0",
    "apollo-server": "^3.8.2",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-graphql": "^0.12.0",
    "mongoose": "^6.3.5",
    "prisma": "^3.14.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.40",
    "nodemon": "^2.0.16",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  }
}
